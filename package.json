{
  "name": "ib-trading-platform",
  "version": "0.0.1",
  "private": true,
  "description": "Root tooling for the shared ESLint/Prettier config. backend/ and ui/ are independent packages with their own dependencies; only the linting toolchain lives here.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.19.1",
    "@typescript-eslint/parser": "^8.19.1",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.1",
    "prettier": "^3.4.2"
  }
}
